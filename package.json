{
  "name": "telegram-echo-bot",
  "version": "1.0.0",
  "description": "A simple echo bot for Telegram",
  "main": "bot.js",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node ./build/bot.js"
  },
  "devDependencies": {
    "@types/node": "^18.16.3",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "grammy": "^1.16.0"
  }
}
